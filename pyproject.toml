[tool.black]
line-length = 88
skip-numeric-underscore-normalization = "true"
skip-string-normalization = "true"  # temporary workaround for boolean values breaking toml parser
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.venv
)/
'''
target_version = ['py36']
